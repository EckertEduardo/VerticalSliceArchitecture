namespace VerticalSliceArchitecture._4.Shared
{
    public static class ErrorMessages
    {
        public static string Validation(string source) => $"{source}.Validation";
        public static string NotFound(string source) => $"{source}.NotFound";
    }
}
